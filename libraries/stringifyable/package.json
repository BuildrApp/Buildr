{
    "name": "@buildr/stringifyable",
    "version": "0.1.0",
    "devDependencies": {
        "typescript": "^3.3.3333",
        "rimraf": "^2.6.3"
    },
    "scripts": {
        "build": "rimraf ./lib/ && tsc",
        "test": "jest --config jest.config.js"
    }
}
